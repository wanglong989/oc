extends ../layout
block content
    .container
        .wrap-left
            .row
                each item in article_list
                    .col-md-2
                        .thumbnail
                            a(href="/detail/#{item._id}")
                            img
                            .caption
                                h6 #{item.title}
                                p: a.btn.btn-primary(href="/detail/#{item._id}",role="button") 查看详情
        .wrap-right #{title}

            //extends ../includes/right/index
            //block content